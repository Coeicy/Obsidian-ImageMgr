# 📖 功能详解

## 目录

- [图片管理](#-图片管理)
- [批量重命名](#-批量重命名)
- [引用追踪](#-引用追踪)
- [笔记内拖拽调整尺寸](#-笔记内拖拽调整尺寸)
- [MD5 去重](#-md5-去重)
- [回收站管理](#-回收站管理)
- [文件保护](#-文件保护)
- [操作日志](#-操作日志)
- [拖拽框选](#-拖拽框选)
- [图床上传](#-图床上传)
- [配置备份](#-配置备份)
- [移动端适配](#-移动端适配)
- [完整功能清单](#-完整功能清单)

---

## 📸 图片管理

### 智能扫描

- 自动扫描仓库中的所有图片文件（PNG、JPG、GIF、WEBP、SVG、BMP）
- 支持增量扫描，仅扫描新增或修改的文件，速度提升 50-80%
- 链接信息预计算：空链接和链接格式统计在扫描时预计算并缓存
- 智能刷新：检测文件变化，无变化时只刷新显示，不重新扫描

### 搜索与筛选

- **实时搜索**：快速搜索图片名称和路径
- **灵活排序**：按名称、大小、日期、尺寸、引用数量等排序
- **类型筛选**：按图片类型筛选（PNG、JPG、GIF 等）
- **位置筛选**：按位置类型筛选（🌩️ 云端图片 / 💾 本地图片）
- **锁定筛选**：按锁定状态筛选（已锁定 / 未锁定）
- **引用筛选**：按引用状态筛选（被引用 / 未被引用）
- **多级排序**：支持组合多个排序条件
- **倒序清除**：按操作顺序倒序清除搜索、排序、筛选、分组

### 网络图片支持

- **智能扫描控制**：支持开启/关闭网络图片扫描功能
- **自动扫描**：自动发现笔记中的网络图片引用（http/https）
- **混合预览**：在主视图中与本地图片混合展示，带🌩️标识
- **智能加载**：三级容错加载机制（直连 -> 本地代理 -> 公共代理）
- **扫描方式**：
  - 命令面板：按 `Ctrl/Cmd + P`，输入 `扫描网络图片`
  - 文件夹右键：在文件资源管理器中右键文件夹
  - 文件右键：在文件资源管理器中右键 Markdown 文件
- **图床上传**：支持7种图床（SM.MS、Imgur、GitHub、七牛云、阿里云、腾讯云、又拍云）
- **网络图片缓存系统**（IndexedDB 存储）：
  - **存储位置**：浏览器 IndexedDB + 插件文件夹 `network-image-cache/`
  - **四类缓存数据**：
    - **网络图片记录（images）**：URL、来源文件、行号、验证状态等
    - **已扫描文件索引（files）**：文件路径、修改时间、内容哈希
    - **系统元数据（metadata）**：扫描时间、统计信息
    - **失效 URL 黑名单（blacklist）**：失效链接记录
  - **清理策略**：LRU 清理、TTL 清理、孤立记录清理

### 智能分组

- **按文件夹**：根据图片所在目录自动分组
- **按类型**：按图片格式（PNG、JPG等）分组
- **按引用状态**：区分已引用和未引用的图片
- **按锁定状态**：按文件保护状态分组
- **按位置类型**：区分🌩️云端图片和💾本地图片
- **自定义分组**：手动创建和管理分组

### 图片预览

- 美观的网格布局预览界面
- 支持懒加载，流畅处理大量图片
- 自适应卡片大小和间距
- 显示图片名称、大小、尺寸、序号、锁定图标等信息

---

## 🏷️ 批量重命名

支持两种重命名模式：

### 普通重命名

使用占位符批量命名：
- `{index}` - 自动编号（001, 002...）
- `{name}` - 原文件名
- 示例：`image_{index}` → `image_001.jpg`

### 智能重命名

根据引用笔记自动命名：
- 自动查找图片的引用笔记
- 根据笔记路径和图片序号生成命名
- 支持多引用处理策略
- 可配置路径命名深度

---

## 🔗 引用追踪

### 多格式支持

自动检测图片在笔记中的引用，支持：
- **Markdown 链接**：`![](image.png)`、`![alt](image.png)`
- **Wiki 链接**：`![[image.png]]`、`![[image.png|alt]]`
- **HTML 标签**：`<img src="image.png">`、`<img src="image.png" width="100">`
- **单行多引用**：精准解析同一行中的多个图片引用

### 引用管理

- **引用查询**：自动查找图片在笔记中的引用
- **引用更新**：自动更新笔记中的图片链接
- **引用信息缓存**：预计算并缓存引用信息
- **实时引用更新**：笔记引用变化时自动更新缓存
- **链接格式统计**：预计算各种链接格式的数量

### 空链接检测

- **双标签页设计**：本地链接和网络链接分开显示
- **本地链接**：检测指向不存在文件的图片链接
- **网络链接**：检测失效的网络图片链接（404、DNS错误等）
- **搜索筛选**：支持按文件路径、链接文本实时搜索过滤
- **一键跳转**：点击行号徽章直接跳转到对应笔记
- **重新缓存**：网络链接支持点击🔄按钮重新尝试缓存

### 链接格式转换

批量转换图片链接格式：
- **尽可能简短**：仅使用文件名
- **相对路径**：基于当前笔记的相对路径
- **绝对路径**：从仓库根目录开始的完整路径
- 自动读取 Obsidian 的"新链接格式"设置
- 保留原有的显示文本和尺寸信息

---

## 📐 笔记内拖拽调整尺寸

在阅读视图与编辑视图中为图片添加右下角拖拽手柄：

### 核心特性

- **双视图支持**：阅读视图 + 编辑视图
- **智能图片匹配**：通过 URL 解析和文件名匹配
- **实时预览**：编辑视图中拖拽时即时更新图片尺寸

### 写回格式

- **Wiki 链接**：`![[image.png|显示文本|100x200]]`
- **HTML 标签**：`<img src="image.png" width="100" height="200">`
- **Markdown 链接**：转为带尺寸的 HTML `<img>` 标签

### 支持的图片类型

- **本地图片**：PNG、JPG、JPEG、GIF、BMP、WEBP、SVG
- **网络图片**：HTTP/HTTPS 链接

### 使用方式

1. 在「设置 → 图片操作」中开启功能
2. 将鼠标悬停在图片上，拖动右下角的拖拽手柄
3. 可选开启「保持宽高比」
4. 拖拽结束后自动保存

---

## 🔄 MD5 去重

- 通过 MD5 哈希值检测**本地**重复图片
- **仅针对本地图片**：云端图片为外链不占本地空间
- 哈希缓存管理，自动缓存计算结果
- 显示重复图片列表，支持批量删除

### 🔑 哈希值的作用

| 类型 | 算法 | 对象 | 主要作用 |
|------|------|------|----------|
| **云端图片 ID** | SHA-256 | 文件名+URL 组合 | 缓存主键、操作记录追踪 |
| **MD5 哈希** | MD5 | 图片文件内容 | 重复检测、锁定列表、操作日志 |

---

## 🗑️ 回收站管理

安全的删除机制：
- **删除保护**：支持移至系统回收站
- **插件回收站**：删除的图片移至 `.trash` 目录
- **批量恢复**：恢复删除的图片
- **永久删除**：彻底删除文件
- **清空回收站**：一键清空所有已删除文件
- **MD5 缓存**：自动计算并缓存 MD5 值

---

## 🔒 文件保护

锁定重要文件防止误操作：
- **三要素精确匹配**：MD5 + 文件名 + 路径
- **重复检测**：重复文件不会被误锁定
- **批量操作保护**：批量操作自动跳过锁定文件
- **快速标记**：快速锁定/解锁文件
- **统一管理**：通过设置页面集中管理

---

## 📜 操作日志

完整的操作追踪系统：
- **日志级别**：DEBUG、INFO、WARNING、ERROR
- **操作类型**：扫描、重命名、移动、删除、锁定、引用更新等
- **详细信息**：记录旧值、新值、影响的笔记、行号等
- **图片追踪**：基于 MD5 哈希追踪图片的完整操作历史
- **日志查询**：支持按时间、级别、操作类型筛选
- **导出功能**：支持导出日志为 JSON 格式

---

## 🖱️ 拖拽框选

- 类似文件夹的鼠标拖拽框选多个图片
- 框选时自动更新复选框状态
- 支持在图片管理页和回收站页框选
- 点击空白区域可取消选中

---

## ☁️ 图床上传

支持将网络图片批量上传到云端图床，自动替换笔记中的链接。

### 支持的图床（按易用性排序）

| 图床 | 难度 | 所需配置 | 特点 |
|------|------|----------|------|
| **SM.MS** | ⭐ | API Token | 最简单，免费/付费图床 |
| **Imgur** | ⭐⭐ | Client ID | 国外主流图床 |
| **GitHub** | ⭐⭐⭐ | Token + 仓库 | 免费，可配合 CDN 加速 |
| **七牛云** | ⭐⭐⭐⭐ | AK + SK + Bucket | 国内稳定 |
| **阿里云** | ⭐⭐⭐⭐ | AK + SK + Bucket | 企业级服务 |
| **腾讯云** | ⭐⭐⭐⭐ | SecretId + SecretKey | 国内大厂 |
| **又拍云** | ⭐⭐⭐⭐ | 操作员账号 + 密码 | 专业云存储 |

### 使用方式

1. 进入「设置 → 网络图片 → 图床上传配置」
2. 启用想要使用的图床，填写对应配置
3. 扫描网络图片
4. 在扫描结果中选择图片，点击上传按钮
5. 上传成功后自动替换笔记中的原始链接

---

## 💾 配置备份

完善的设置导入导出功能：

### 功能特点

- **完整备份**：备份插件所有设置项，包括图床配置
- **敏感信息保护**：默认过滤 API 密钥等敏感字段
  - SM.MS: `apiToken`
  - Imgur: `clientId`
  - GitHub: `token`
  - 七牛云: `accessKey`, `secretKey`
  - 阿里云: `accessKeyId`, `accessKeySecret`
  - 腾讯云: `secretId`, `secretKey`
  - 又拍云: `password`
- **选择性导出**：可选择是否包含敏感信息
- **导入前备份**：自动备份当前配置

### 使用方式

1. **导出配置**：设置 → 扩展功能 → 导出配置
2. **导入配置**：选择 JSON 文件 → 可选覆盖现有配置

---

## 📱 移动端适配

### 响应式布局

- **桌面端**（≥ 1200px）：5列图片布局
- **平板端**（768px - 1199px）：3列图片布局
- **手机横屏**（480px - 767px）：2列图片布局
- **手机竖屏**（< 480px）：1列图片布局

### 移动端优化

- 工具栏自适应布局
- 图片卡片信息智能隐藏
- 文件名自动换行
- 触摸友好：按钮最小 32px × 32px
- 模态框响应式布局

### Android 相册隐藏

- 创建 `.nomedia` 文件防止 Android 相册扫描图片

---

## ✅ 完整功能清单

### 核心功能

#### 图片管理
- ✅ 智能扫描 - 自动扫描仓库中的所有图片文件
- ✅ 实时搜索 - 快速搜索和过滤图片
- ✅ 灵活排序 - 按名称、大小、日期、尺寸等排序
- ✅ 类型筛选 - 按图片类型筛选
- ✅ 位置筛选 - 按云端/本地筛选
- ✅ 锁定筛选 - 按锁定状态筛选
- ✅ 引用筛选 - 按引用状态筛选
- ✅ 详细信息 - 查看图片的完整信息
- ✅ 图片预览 - 美观的网格布局预览界面
- ✅ 性能优化 - 懒加载机制

#### 编辑功能
- ✅ 批量重命名 - 支持灵活的文件重命名模式
- ✅ 智能重命名 - 根据引用笔记路径自动生成命名
- ✅ 图片编辑 - 旋转、翻转、尺寸调整
- ✅ 路径修改 - 支持移动图片到不同目录

#### 高级功能
- ✅ 图片引用查询 - 支持单行多引用精准解析
- ✅ 多格式支持 - Markdown、HTML、Wiki 链接
- ✅ 引用更新 - 自动更新笔记中的图片链接
- ✅ 空链接检测 - 检测指向不存在文件的链接
- ✅ 操作日志 - 专业的日志记录系统
- ✅ MD5 去重 - 检测本地重复图片
- ✅ 回收站管理 - 支持恢复和永久删除
- ✅ 文件保护 - 基于 MD5 哈希值的保护

#### 用户界面
- ✅ 网格布局 - 美观的图片卡片展示
- ✅ 工具栏 - 快速访问常用功能
- ✅ 详情页面 - 高质量图片预览、缩放、旋转
- ✅ 模态框 - 搜索、排序、筛选、分组等

#### 技术特性
- ✅ 模块化架构 - 代码组织清晰
- ✅ TypeScript 严格模式
- ✅ 异步处理 - 使用 async/await
- ✅ 懒加载机制
- ✅ 增量更新
- ✅ 跨平台支持 - Windows、macOS、Linux
